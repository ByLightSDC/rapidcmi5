/** @constant
 * if true UUIDs will resolve to object names
 * @type {boolean}
 * @default
 */
export const featureFlagInspectField = true;

/** @constant
 * if true UUIDs will resolve to object names if field is disabled/readonly
 * @type {boolean}
 * @default
 */
export const featureFlagInspectDisabledField = true;

/** @constant
 * if true UUIDs will present edit icon that can be clicked to access editable form
 * @type {boolean}
 * @default
 */
export const featureFlagInspectModalIsEditable = true;

/** @constant
 * if true editing a record within a parent form should route instead of pop modal
 * @type {boolean}
 * @default
 */
export const featureFlagRouteModals = false;
