# cmi5-build-backend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cmi5-build-backend` to build the library.

## Running unit tests

Run `nx test cmi5-build-backend` to execute the unit tests via [Jest](https://jestjs.io).
