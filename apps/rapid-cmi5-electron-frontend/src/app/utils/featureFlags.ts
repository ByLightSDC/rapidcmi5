/** @constant
 * if true Content Builder will appear in design tools dashboard
 * @type {boolean}
 * @default
 */
export const featureFlagMdXEditor = true;

/** @constant
 * if true Scenario Designer will appear in scenarios & design tools dashboard
 * @type {boolean}
 * @default
 */
export const featureFlagScenarioDesignModes = true;

/** @constant
 * if true Scenario Designer will present UI used to validate scenario assets
 * @type {boolean}
 * @default
 */
export const featureFlagValidateScenarioDesigner = true;

/** @constant
 * if true Visualizations will appear in scenarios dashboard
 * @type {boolean}
 * @default
 */
export const featureFlagVisualizations = true;

/** @constant
 * if true Visualizations will appear in design tools dashboard
 * @type {boolean}
 * @default
 */
export const featureFlagVisualizationSandbox = false;

/** @constant
 * if true ViewModals will hide any modal on unmount
 * @type {boolean}
 * @default
 */
export const featureFlagCleanUpModals = true;

/** @constant
 * if true forms will Bookmarked to allow for editing multiple forms in one sequence
 * @type {boolean}
 * @default
 */
export const featureFlagBookmarks = true;

/** @constant
 * if true Create buttons will appear in list views
 * @type {boolean}
 * @default
 */
export const featureFlagAllowCreateNew = true;

/** @constant
 * if true Multi select will handle "shift" select
 * @type {boolean}
 * @default
 */
export const featureFlagMultipleSelectShift = true;

/** @constant
 * if true, the NetmapViewToggle buttons will include a globe icon that, when
 * clicked, will display the geolocation globe
 */
export const featureFlagGeoLocation = true;
