/** Width of the drag handle element in pixels. */
export const HANDLE_WIDTH = 20;

/** Distance from the viewport edge (in px) at which auto-scroll kicks in during a drag. */
export const SCROLL_ZONE = 80;

/** Pixels scrolled per animation frame during auto-scroll. */
export const SCROLL_SPEED = 10;