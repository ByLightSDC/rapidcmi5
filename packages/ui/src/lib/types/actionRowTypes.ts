export type RowAction = {
  tooltip: string;
  disabled?: boolean;
  hidden?: boolean;
  icon: JSX.Element;
  isButton?: boolean;
};
export type RowStatus = {
  icon: JSX.Element;
  label: string;
};
