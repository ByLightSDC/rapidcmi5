/**
 * @typedef { object} FormStateType
 * @property {any} errors React hook form errors
 * @property {boolean} isValid whether form fields are valid
 */
export type FormStateType = {
  errors: any;
  isValid: boolean;
};
